{
  "name": "learn-javascript",
  "version": "1.0.0",
  "scripts": {
    "start": "live-server . --port=3000 --host=localhost --no-browser"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "prettier": "^3.5.3"
  }
}
